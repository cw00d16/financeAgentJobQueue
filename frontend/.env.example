# Run "terraform output" after deploying infrastructure to get this value

# HTTP API invoke URL — POST /jobs, GET /jobs/{id} (from terraform output api_gateway_url)
REACT_APP_API_URL=https://xxxxxxxxxx.execute-api.us-east-2.amazonaws.com/prod
